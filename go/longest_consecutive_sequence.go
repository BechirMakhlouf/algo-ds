package main


func longestConsecutive(nums []int) int {
	existingNumber := make(map[int]bool)

  for _, num := range nums {
      existingNumber[num] = true
  }
  
  longestConsecutive := 0

  currentConsecutive := 0
  i := nums[0]
  for true {
    if longes
    if val, ok := existingNumber[i]; ok {
      currentConsecutive++
    }
  }
	return 0
}
